const path = require('path');

module.exports = {
  'transpileDependencies': [
    'vuetify'
  ]
};

/* put this code when build */
// 'outputDir': path.resolve(__dirname, '../templates'), // fix this
// 'assetsDir': '../static'
