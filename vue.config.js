const path = require('path');

module.exports = {
  'transpileDependencies': [
    'vuetify'
  ]
};

//* TODO: fix it, error while launching the app with this code snippet
/* put this code when build */
// 'outputDir': path.resolve(__dirname, '../udo-project-api/templates'), // fix this
// 'assetsDir': '../static'
